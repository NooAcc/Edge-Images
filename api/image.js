import { createImageHandler } from '../lib/handler.js';

export { CACHE_CONTROL, PROCESSOR_NAME, sanitizeHeaderValue, createImageHandler } from '../lib/handler.js';

export default createImageHandler();
